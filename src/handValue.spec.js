import { handValue } from './handValue'

describe('HandValue() evaluate hand rate', () => {
    describe.skip('IDK, something...', () => {
            it(`🤔`, () => {
                expect(handValue()).to.be.an('function')
            })
    })
})

